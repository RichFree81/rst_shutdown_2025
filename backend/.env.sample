# Backend environment (sample). Copy this file to backend/.env and fill values.
# Do NOT commit backend/.env; it is gitignored.

APP_ENV=dev

# Admin bootstrap (dev only)
ADMIN_EMAIL=you@example.com
ADMIN_PASSWORD=ChangeMeDev123!

# Security
JWT_SECRET=replace-with-a-strong-random-secret
CORS_ALLOW_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# Optional: override local SQLite with a managed DB (e.g., in Azure)
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:5432/DBNAME
# DATABASE_URL=mssql+pyodbc://USER:PASSWORD@HOST:1433/DBNAME?driver=ODBC+Driver+18+for+SQL+Server&Encrypt=yes&TrustServerCertificate=no

# Optional email settings
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASSWORD=
