APP_ENV=dev

# Optional: FRONTEND URL
FRONTEND_BASE_URL=http://localhost:5175

# Auth/JWT
JWT_SECRET=a_long_random_string
JWT_ALGORITHM=HS256
JWT_EXPIRES_MIN=60

# First admin bootstrap (used at API startup if no users exist)
ADMIN_EMAIL=
ADMIN_PASSWORD=

Feedback submitted

# CORS
CORS_ALLOW_ORIGINS=["http://localhost:5175"]

# SMTP (optional)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=no-reply@example.com